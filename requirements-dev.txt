# ── Development & CI dependencies ──
# Installed only in CI pipelines and local dev, never in production Docker image.
bandit>=1.9.0,<2.0.0
# Pinned exactly, unlike the rest: `ruff format` output changes between
# releases, so a floating ruff can fail the lint job on an unchanged commit.
# Keep in sync with RUFF_VERSION in .github/workflows/lint-and-compile.yml.
ruff==0.16.8
pip-audit>=2.0.0,<3.0.0
pytest>=8.0.0,<9.0.0
pytest-asyncio>=1.0.0,<2.0.0
