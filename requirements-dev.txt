# ── Development & CI dependencies ──
# Installed only in CI pipelines and local dev, never in production Docker image.
bandit>=1.9.0,<2.0.0
ruff>=0.7.0,<1.0.0
pip-audit>=2.0.0,<3.0.0
pytest>=8.0.0,<9.0.0
pytest-asyncio>=1.0.0,<2.0.0
